function childNum(num, count) {
  const numArr = []
  for (let i = 0; i < num; i++) {
    numArr.push(i + 1)
  }
  let p = 0
  while (numArr.length > 1) {
    if (numArr[p] % 3 === 0 ) {
      numArr.splice
    }
    p++
  }
}

childNum(30, 3)
